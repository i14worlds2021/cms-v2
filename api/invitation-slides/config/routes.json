{
  "routes": [
    {
      "method": "GET",
      "path": "/invitation-slides",
      "handler": "invitation-slides.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/invitation-slides",
      "handler": "invitation-slides.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/invitation-slides",
      "handler": "invitation-slides.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
